@model KhumaloCraft_Part2.Models.CartItem

@{
    ViewData["Title"] = "Create";  // Sets the title of the page
}

<h1>Create</h1>

<h4>CartItem</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            @{
                // This form will be posted to the Create action in the controller
            }
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @{
                // Displays validation summary if there are model errors
            }
            <div class="form-group">
                <label asp-for="ProductName" class="control-label"></label>
                @{
                    // Label for the ProductName field
                }
                <input asp-for="ProductName" class="form-control" />
                @{
                    // Input for the ProductName field
                }
                <span asp-validation-for="ProductName" class="text-danger"></span>
                @{
                    // Validation message for the ProductName field
                }
            </div>
            <div class="form-group">
                <label asp-for="Price" class="control-label"></label>
                @{
                    // Label for the Price field
                }
                <input asp-for="Price" class="form-control" />
                @{
                    // Input for the Price field
                }
                <span asp-validation-for="Price" class="text-danger"></span>
                @{
                    // Validation message for the Price field
                }
            </div>
            <div class="form-group">
                <label asp-for="Quantity" class="control-label"></label>
                @{
                    // Label for the Quantity field
                }
                <input asp-for="Quantity" class="form-control" />
                @{
                    // Input for the Quantity field
                }
                <span asp-validation-for="Quantity" class="text-danger"></span>
                @{
                    // Validation message for the Quantity field
                }
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
                @{
                    // Submit button for the form
                }
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
    @{
        // Link to go back to the list of CartItems
    }
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    @{
        // Includes the validation scripts partial view
    }
}